server:
  port: 5800
spring:
  rabbitmq:
    port: 5672
    username: guest
    password: guest
  servlet:
    multipart:
      file-size-threshold: 1MB
      location: C:/Temp
      max-file-size: 100MB
      max-request-size: 100MB
  datasource:
    url:   jdbc:h2:tcp://localhost/~/chatt
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jwt:
    secret: wonseok1234!
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        #show_sql: false
        format_sql: true
        default_batch_fetch_size: 100
 # redis:
 #   host: localhost
 #   port: 6520
 # session:
 #   store-type: redis
#logging:
 # level:
 #   org.hibernate.SQL: debug
#    org.hibernate.type: trace #쿼리 파라미터

